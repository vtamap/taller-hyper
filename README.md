Para ejecutar el proyecto, puedes usar el siguiente, se
ejecutara en el puerto 8081:
--
mvn spring-boot:run
--


Si necesitas algún puerto especial, puedes usar el siguiente comando:

--
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=1919"
--
